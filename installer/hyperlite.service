[Unit]
Description=Hyperlite API (FastAPI + libvirt)
After=network-online.target libvirtd.service
Wants=network-online.target
Requires=libvirtd.service

[Service]
Type=simple
WorkingDirectory=/root/hyperlite
EnvironmentFile=/root/hyperlite/.env
Environment=PYTHONUNBUFFERED=1
Environment=HOME=/root
# Fichiers temporaires des envois multipart (ISO de plusieurs Go) sur le
# disque de donnees, PAS dans /tmp : sur Debian recent /tmp est un tmpfs
# (RAM/2), donc un ISO plus gros que lui echoue avec "error parsing the body"
# (bug reel serveur-antho, RHEL 10 DVD > 7,8 Go de tmpfs).
Environment=TMPDIR=/root/hyperlite/data/tmp
ExecStartPre=/bin/mkdir -p /root/hyperlite/data/tmp
ExecStartPre=/root/hyperlite/scripts/ensure-tls-cert.sh
ExecStartPre=/root/hyperlite/scripts/write-motd.sh
ExecStart=/root/hyperlite/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000 --ssl-keyfile /root/hyperlite/data/tls/hyperlite.key --ssl-certfile /root/hyperlite/data/tls/hyperlite.crt
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
